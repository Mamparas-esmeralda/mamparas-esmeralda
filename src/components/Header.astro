---
import IconClose from "./icons/IconClose.astro";
import IconMenu from "./icons/IconMenu.astro";
---

<header class="fixed top-0 w-full bg-white">
  <div
    class="relative px-[var(--px-sm)] flex justify-between items-center w-full"
  >
    <a href="/">
      <img src="/public/images/logo.png" class="max-h-[10vh]" alt="" />
    </a>
    <div class="cursor-pointer sm:hidden transition-[0.5s]" id="menu-icon">
      <div id="open">
        <IconMenu />
      </div>
      <div id="close" class="hidden">
        <IconClose />
      </div>
    </div>
    <div
      class="shadow-sm bg-gradient-to-tr from-slate-600 to-[var(--color-primary)] p-10 flex justify-start translate-x-full absolute top-[100%] transition-[0.5s] right-0 w-5/6 h-screen bg-white z-50"
      id="menu"
    >
      <ul class="flex gap-4 flex-col text-white font-bold text-xl">
        <li>Home</li>
        <li>La empresa</li>
        <li>Productos</li>
        <li>Contacto</li>
      </ul>
    </div>
  </div>
</header>

<script>
  const menuIcon = document.getElementById("menu-icon");
  const close = document.getElementById("close");
  const open = document.getElementById("open");
  const menu = document.getElementById("menu");

  menuIcon.addEventListener("click", () => {
    menu.classList.toggle("translate-x-full");
    close.classList.toggle("hidden");
    open.classList.toggle("hidden");
  });
</script>
